# Project TODO

- [ ] Inspect the supplied `commonground-1.0.0.apk` and document its app identity, package metadata, and observable feature set. Blocked: uploaded APK archive is corrupted; needs a valid re-upload.
- [x] Verified that the supplied APK was not generated by this project and confirmed the new Cooperative Gig Services workspace as the implementation baseline.
- [x] Define shared domain models for users, worker profiles, services, bookings, chats, messages, reviews, notifications, and complaints.
- [x] Configure the Cooperative Gig Services visual system and portrait-first responsive mobile layout.
- [x] Build splash, login, registration, password recovery handoff, role selection, and logout flows using a local prototype session pending Firebase authentication integration.
- [x] Build customer home, services search, category filtering, nearby worker list, and worker-profile experiences.
- [x] Build booking creation, booking confirmation, booking detail, and booking-history status views with booking-gated contact controls.
- [x] Build guarded customer-worker chat and booking-gated device-call actions using the device dialer when a valid job is active.
- [x] Build worker dashboard, availability controls, request management, active-job workflow, earnings, and profile editing.
- [x] Build rating and review submission, worker rating summaries, and one-review-per-booking validation.
- [x] Build admin views for customers, workers, verification, services, bookings, complaints, reports, and platform statistics.
- [ ] Integrate secure authentication, Firestore data access, Storage media upload, maps/location permissions, and push-notification events.
- [x] Add Firebase security rules and operational validation for role and booking access. Firestore rules are published in the user-owned project.
- [x] Generate custom product icon, install it in all required app icon locations, and update application branding configuration.
- [x] Write deterministic tests for booking-state transitions, Firebase configuration, and contact guards.
- [x] Validate TypeScript, booking lifecycle tests, Firebase configuration, and Firebase Functions source; native device testing remains a release-stage task.
- [ ] Configure all production data services under the user's own Firebase and Google Cloud accounts, with no application data stored in a platform-owned database. Pending: optional Storage billing activation, restricted Google Maps key, and deployed Firebase Functions/FCM credentials.
- [x] Provide a complete downloadable project folder containing source code, configuration, Firebase rules, setup guide, and test files.
- [x] Document the user-owned Firebase project connection, access control, backup, and release-build setup.
- [x] Investigated and fixed the reported Firebase account-creation failure by removing administrator self-registration, aligning public roles with Firestore Rules, and adding clear Firebase error feedback; TypeScript and registration-policy tests pass.
- [ ] Confirm customer and worker account creation in the user’s live Firebase project on a physical device or refreshed app preview.
- [ ] Reproduce the persistent account-creation error in the shared app preview and correct the production Firebase registration path.
- [ ] Replace the confirmed invalid Firebase API key in the mobile-app environment and verify that Firebase Authentication accepts a new account request.
- [ ] Diagnose and fix stale Firebase API-key propagation in the active preview bundle after the user’s refreshed retest still returned `auth/api-key-not-valid`.
- [x] Make Online now, Verified, and Top rated worker filters return the corresponding worker sets and show a clear no-results state when appropriate.
- [x] Replace customer and worker profile placeholder alerts with editable personal, service, availability, address, notification, and account-detail forms that save locally and to Firebase when connected.
- [x] Guard remote push-notification initialization in Expo Go so local Android testing can proceed without a native notification runtime error.
- [x] Make Upcoming, Active, Completed, and Cancelled booking tabs filter the customer booking list correctly, including a clear empty state.
- [x] Add precise Firebase existing-account sign-in feedback for incorrect credentials, API configuration, disabled Email/Password, network, throttling, and Firestore permission cases.
- [ ] Confirm the exact Firebase error code and successful sign-in for the user's existing account after applying the latest local project update.
- [ ] Refresh the user's local project from the latest checkpoint and verify that Upcoming, Active, Completed, and Cancelled each display only their matching bookings.
- [ ] Replace the invalid Firebase API key in the user’s local `.env` with the newly created Google Cloud client key and verify Firebase Authentication sign-in.
- [ ] Verify and correct the active Expo bundle because booking tabs still display unfiltered content on the user’s device after the latest local update attempt.
- [x] Replace the fixed Hyderabad header with automatic device location detection, a manual location chooser, persisted selection, and a graceful fallback when permission is denied.
- [x] Replace the fixed customer “Good morning” label with a greeting that changes by local time.
- [x] Remove the “Local skills. Shared opportunity.” text from the login landing screen.
- [ ] Restore the latest local folder’s locked Expo SDK 54 dependencies and start it without npx installing Expo 57.
- [ ] Confirm the user can resume testing from the previously working downloaded folder while retaining the latest folder for recent fixes.
- [ ] Bypass the Expo CLI package-metadata network fetch failure in local folder (3) and verify the latest app starts.
- [ ] Resolve the Expo Go “Bundling 100%” freeze reproduced from both local folders and confirm on-device loading.
- [ ] Start the refreshed Expo Go session from local folder (3) and verify the latest booking filters, time-based greeting, and location chooser.
- [x] Fix unresponsive customer location-selector and booking-status-tab taps in the latest device build.
- [x] Align the user’s local Expo SDK 54 patch packages to resolve the persistent Expo Go “Reloading…” loop.
- [ ] Verify the current local source version and resolve booking-filter and location-selector interactions still failing after successful login.
- [x] Create and deliver a clean ZIP archive of the latest verified source for local Windows setup.
- [ ] Create a separate restricted Google Maps API key and securely configure it for the Android/iOS map release build.
- [x] Publish authenticated worker profiles to Firestore and display them in the customer worker directory in real time.
- [x] Create and deliver an updated clean ZIP archive containing the live worker-directory implementation.
- [x] Prevent undefined optional worker profile fields from being written to Firestore during account creation.
- [x] Create and deliver a clean ZIP archive containing the corrected Firestore worker-registration flow.
